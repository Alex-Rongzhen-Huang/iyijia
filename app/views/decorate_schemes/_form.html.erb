<%= form_for(@decorate_scheme) do |f| %>
  <% if @decorate_scheme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@decorate_scheme.errors.count, "error") %> prohibited this decorate_scheme from being saved:</h2>

      <ul>
      <% @decorate_scheme.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

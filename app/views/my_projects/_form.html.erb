<%= form_for(@my_project) do |f| %>
  <% if @my_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_project.errors.count, "error") %> prohibited this my_project from being saved:</h2>

      <ul>
      <% @my_project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :order %><br />
    <%= f.text_field :order %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

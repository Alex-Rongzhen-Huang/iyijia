  <p id="notice"><%= notice %></p>
  
  <div class="row col-md-12" style="padding-top: 40px">
    <h2 style="padding-left: 20px;" align="center"><%= @my_project.name %></h2>
    <br>
      <div class="row">
        <% @my_project.my_project_items.each do |x| %>
          <div class="col-xs-6 col-md-3">
            <a href="<%= x.path %>" download class="thumbnail"><img src="<%= x.path %>" class="image_keep_ratio_for_decorate_schemes"></a>
            <p style="text-align: center"><%= x.description %></p>
          </div>
        <% end %>
      </div>
      <div align="center">
      <a class="btn btn-primary btn-lg" href="<%= user_orders_path %>">返回</a>
      </div>
      <br>
      <br>     
  </div>